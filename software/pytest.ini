[pytest]
minversion = 8.0
addopts = -ra --strict-markers --cov=controller_app --cov-report=term-missing
testpaths = tests
qt_api = pyqt6
markers =
    ui: тесты Qt UI (требуют offscreen-плагин)
    integration: интеграционные тесты с моками транспорта